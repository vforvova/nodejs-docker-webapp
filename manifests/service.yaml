kind: Service
apiVersion: v1
metadata:
  labels:
    app: vova-nodejs-docker-webapp
  name: vova-nodejs-docker-webapp
  namespace: workshop

spec:
  type: NodePort
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: vova-nodejs-docker-webapp
